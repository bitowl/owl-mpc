{
    "type": "object",
    "properties": {
        "host": {
            "type": "string",
            "default": "localhost",
            "description": "The host mpd is running on"
        },
        "port": {
            "type": "number",
            "default": 6600,
            "description": "The port mpd is running on"
        },
        "reconnectTime": {
            "type": "number",
            "default": 10000,
            "description": "Time between reconnect tries (milliseconds)"
        },
        "showBanButton": {
            "type": "boolean",
            "default": false,
            "description": "Show a button that skips the current song and removes it from the 'stream' playlist."
        },
        "showVolumeSlider": {
            "type": "boolean",
            "default": true,
            "description": "Show a slider to control the volume"
        }
    },
    "required": [
    ]
}